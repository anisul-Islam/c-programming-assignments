#include <stdio.h>

int main() {
    char character = 'A'; // Change this to the character you want to find the ASCII value of
    printf("ASCII value of %c: %d\n", character, character);
    return 0;
}
